# BigLocalApps / BIM (L4–L5)

- **Skills MD** : `BigLocalApps/skills/blender/**` → porter progressivement dans `expertise/skills/` et `expertise/recipes/`.
- **Conductors / extensions BIM** : restent dans BigLocalApps ou addon image ; exposés plus tard comme tools métier ou persona.
- **Context detection** (mode/workspace) : inspirer `get_blender_context` (déjà outillé côté guidance).
