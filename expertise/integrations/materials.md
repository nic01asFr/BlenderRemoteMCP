# Materials — profil natif

| Élément | Statut |
|---------|--------|
| `skill://materials` | Enrichi |
| `mat_lib` (6 presets) | Fait |
| `mat_list_presets` / `mat_apply_preset` | Fait |
| Recipe `mat_studio_presets` | Fait |
| Textures image / UDIM | Plus tard |

Couplage GN : après `gn_run_template`, `mat_apply_preset` sur l’objet hôte.
